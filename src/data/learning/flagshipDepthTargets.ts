/**
 * Editorial targets for instructional depth audits — distinct from {@link MIN_OVERRIDE_CHARS}
 * merge gate in flagshipSessionContentResolve.ts.
 */
/** Premium resolved-content refinement target (chars across merged learner-visible blocks). */
export const PREMIUM_DEPTH_TARGET_CHARS = 1900
