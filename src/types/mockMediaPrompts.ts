/** Structured prompts for future static / motion / video pipelines (mock-only for now). */
export type MockMediaPrompts = {
  image_prompt: string
  poster_prompt: string
  animation_prompt: string
  storyboard_summary: string
}
