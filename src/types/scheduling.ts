/**
 * Placeholder lifecycle for opportunity-led posts (human-in-the-loop by default).
 * Scheduling and multi-platform publishing plug in here later.
 */
export type ContentLifecycleStage = 'opportunity' | 'draft' | 'scheduled' | 'published'
