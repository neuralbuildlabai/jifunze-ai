# Supabase CLI project config. Set project_id when linking: `supabase link`.
# https://supabase.com/docs/guides/cli/config

[functions.generate-content]
# Keep auth enforced for production/UAT generation.
verify_jwt = true
