# Copy this file to .env and fill in your real key.
# .env is gitignored — do not commit it.

OPENAI_API_KEY=sk-your-key-here

# Optional: override the model used by all examples (default: gpt-4o)
# MODEL=gpt-4o
